import styled from 'styled-components';

const Button = styled.button`
  background: tomato;
  border-radius: 3px;
  border: none;
  color: white;
  padding: 10px 25px;
  font-size: 20px;
`;

export default Button;